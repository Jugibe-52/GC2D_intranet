# Editable research environment. Runtime compatibility ranges and optional
# dependency groups are declared in pyproject.toml; use the committed
# constraints file to select the repository's tested direct versions.
-c constraints.txt
-e .[dev,notebook]
